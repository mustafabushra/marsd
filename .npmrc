# npm configuration to ensure consistent behavior across environments

# Strict lifecycle script execution (prevent permission issues)
scripts-prepend-node-path=true

# Always save exact versions
save-exact=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Ensure bin scripts retain execute permissions
# (npm 9+ should handle this, but be explicit)
