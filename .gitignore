dist/
node_modules/
.next/
.env
.env.local
# Holds a real CLERK_SECRET_KEY — never commit. Template: .env.clerk.example
.env.clerk
.vercel
marsad-staging.bundle
# Allow production env vars for Vercel
!.env.production

# Database credentials for scripts/run-migration.mjs — never commit
.env.migrations
